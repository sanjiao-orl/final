# 现状

> 2026-08-16 重写,取代《路线偏移与最新进展》(已归档 `docs/archive/`)。本件只记当前事实与下一步;历史过程查归档与 git 日志。

## 产品

网文连载单作者本地 AI 写作工作台(小说领域的 Cursor):Windows 桌面、纯本地、无账号、BYOK 双模型。需求基线 `01-产品定义.md`(原则:人的方向 AI 的笔 / 结构即智能 / 可配置默认否)。

北极星:作者用本软件写完 3 章可投稿水准的章节,以真实写作验收。止损线:长期无动力用它写完一章,判定假设不成立,体面终止(可剥离资产:账本/core sidecar/领域工具)。

## 架构

`core/`(Node sidecar:AI SDK v7 + MCP client + node:sqlite 会话 + 127.0.0.1 HTTP/SSE)+ `domain/`(MCP stdio 领域服务,22 工具)+ `shell/`(Tauri 2 + Svelte 5 + TipTap,v5 五栏 + 作者笔记)。提示词与 skill 统一 md 文件机制(`core/prompts/`,改文件即生效;决策 0008)。决策记录 `decisions/0001-0008`。

## 当前状态

- 已发布 v0.2.0(壳 v5:写作体验批 + 作者笔记 + 上下文栏 + 快照浏览器);桌面 novel-ws 经 updater 真实通道更新(`scripts/release.mjs` → gh release)。
- 已提交未发布(`436579a`):安全修复批(CSP 兜底 / ready 行 token 脱敏 / apiKey 打码占位即保留 / 账本路径白名单)+ 版本号五处一致(`check-versions.mjs`)+ prompt/skill 文件机制(预置 skill:去AI味润色、章节体检)。
- 验证基线:`npm run check` 0 错(含版本一致性)、`npm test` 456 全绿、`cargo test` 16 绿。
- 流程:squad 路由;完成 = 改完必须验证(测试/构建/实跑),主 agent 审核不盲信回馈(独立二审 2026-08-16 取消,审核手册已归档)。

## 下一步

1. **批三 · 内在深化主线(暂停,待作者指令)**:① 审阅闭环真实写作验收(BLOCKER 标红→清零→冷读过真实章节);② 数据模型重建收尾。按写作反馈切批,每批以亲写一章收尾。
2. **发布 0.2.1 前亲测**:CSP 下各功能正常;真实安装包首启 prompts 释放链路。
3. 审计遗留(未修,按优先级):CORS `*` + dev 端点免鉴权 + token 走 URL 查询串(均 dev 面);壳传 workDir 未清洗拼系统提示;dev 裸跑 domain 默认 npx 命令 Windows 不可用;release.mjs 不校验工作树/不打本地 tag;审批名单与目录约定多处散落无双写源;mcp.ts env 整份透传可收紧白名单。
4. 缓做:skill 管理 UI、壳内指令模板搬家(SelectionPopover/整改模板)、prompt 热重载、工具结果回放字符预算校调。

## 明确不做

图形化世界观、多人协作、模型市场、EPUB 排版、AI 绘图、治理体系、跨卷移动。

## 决策速查(`decisions/`)

做判断前先查这里,每条一句:
- 0001 harness 选型:Vercel AI SDK v7,不手写 agent 内核
- 0002 接线:Node sidecar + HTTP/SSE,不过 Tauri IPC
- 0003 壳基线:Tauri 2 + Svelte 5 + TipTap
- 0004 数据模型与领域服务:domain MCP(结构即智能的载体)
- 0005 暂存区与讨论存区:归属 core sqlite
- 0006 修复轮:周 2-3 审核后质量修复
- 0007 协议契约 v1:/v1/ 版本化 + 进程握手 + event_pump
- 0008 提示词与 skill 统一文件机制:md 文件单一事实源,改文件即生效

## 文档地图

- 活跃:`01-产品定义.md`、本件、`decisions/`。
- 历史:`docs/archive/`(诊断 00 / roadmap / 壳重设计系列 / v4 / v5 / 下一步方案 / 审核手册 / 生态调研 / 路线偏移与最新进展)。
