# 现状

> 2026-08-16 重写，取代《路线偏移与最新进展》（已归档 `docs/archive/`)。本件只记当前事实、规范与待办；历史过程查归档与 git 日志。维护纪律：每批收口后更新本件，写完即提交。2026-08-17 更新：确立新阶段（决策 0011），待办整体替换；铁律回归批（chat 正文进暂存区，决策 0012）收口；同日晚：第 0 步行为数据挖掘以作者访谈替代（0011 修订），批一新增碰撞模式、批二对账器升级为碰撞地基。2026-08-20 更新：吸收人机碰撞方法论方案定稿（决策 0013）——裁决留痕/写前闸门/证据锚/壳白名单入档，批一顺序修订（回放预算提级至碰撞之前）；外部路线 B（人机碰撞版 v1.2.1）冻结为方法论库；批一① 角色与方案收口（0010/0013 落地，protocol 升 2），碰撞模式设计输入扩为「讨论沉淀层」（见待办批一③备注）；批一② 工具回放预算校调收口（落库全量 20k 防爆 / 回放内容档 3000·其余 500 / 总预算 25k→40k）；批一③ 碰撞模式收口（mode=collide 四节输出+壳对比色容错渲染、裁决留痕 decisions.md、章 frontmatter blueprint、讨论沉淀注入，protocol 升 3）。2026-08-21 更新：五路取证审计 + 审计修复批收口（路径守卫统一/sidecar 生命周期看护/版本第六处/竞态与状态机，详见当前进度「审计修复批」）；同日 MSI 打包通道弃用（targets 收 NSIS,WiX 1252 码页装不下中文 prompts 目录名）。

## 产品

网文连载单作者本地 AI 写作工作台（小说领域的 Cursor):Windows 桌面、纯本地、无账号、BYOK 双模型。需求基线 `01-产品定义.md`（原则：人的方向 AI 的笔 / 结构即智能 / 可配置默认否）。

验收口径：以作者真实写作为唯一验收（作者已用本软件持续写作多章；原北极星「写完 3 章可投稿水准章节」已达成，2026-08-16 起移除）。止损线：长期无动力用它写完一章，判定假设不成立，体面终止（可剥离资产：账本/core sidecar/领域工具）。

## 架构

`core/`(Node sidecar:AI SDK v7 + MCP client + node:sqlite 会话 + 127.0.0.1 HTTP/SSE)+ `domain/`(MCP stdio 领域服务，26 工具）+ `shell/`(Tauri 2 + Svelte 5 + TipTap,v5 五栏 + 作者笔记）。提示词与 skill 统一 md 文件机制（解析序 NOVEL_PROMPT_DIR > 随包释放目录，dev 直读 `core/prompts/`;mtime 热重载，改文件即生效；决策 0008)。决策记录 `decisions/`（速查见文末）。

## 当前进度

- **已发布 v0.2.3**(2026-08-17)：铁律回归批（决策 0012，1dfe6ed）——chat 正文一律经本地工具 `stage_chapter_proposal` 进暂存区（候选模型加 kind=replace/append/replace_all，旧库迁移），采纳才落盘；write_chapter 仅作者明确直写时用（审批门保留）；设定路由（设定/伏笔/知情→ledger_upsert、书级元数据→write_meta、严禁写 manuscript，write_chapter/create_chapter 描述加反向边界）；壳采纳按 kind 分派 + 暂存卡徽标 + 提案后自动刷新。验证：581 单测 + cargo + e2e 7/7 + 实模型三探针。发布经真实通道：gh 传资产秒级，NSIS+MSI+sig+latest.json 全部就位。
- **已发布 v0.2.1**(2026-08-16)：壳 v5（写作体验批/作者笔记/上下文栏/快照浏览器）+ 批一安全修复（CSP 兜底/ready 行 token 脱敏/apiKey 打码占位即保留/账本路径白名单）+ prompt/skill 文件机制（预置：去AI味润色、章节体检）+ 低危跟进（domain 子进程 env 白名单、冷读模板单趟替换防二次注入、兜底对齐）+ 文档归整。
- **已发布 v0.2.2**(2026-08-17)：审计收口（workDir 清洗拒目录名注入系统提示、prompt/skill mtime 热重载、/v1/dev 仅 dev 开放、CORS 白名单反射收窄、裸联调 token localStorage 记忆、阈值单源、e2e-workflow 7 步）+ 批三-1 审阅处置闭环 + 批三-2 账本表达力 + 批三-3 内联注入（账本按章切片/声口摘要/壳联动/候选 skill 试跑架）+ 更新体验修复（安装前杀 sidecar 消 NSIS 文件占用弹窗，随本版起生效）。发布过程中发现并修复 release.mjs 资产上传 404 根因（`gh release view --json id` 返回 node_id 非数字 id），改 REST 列表按 tag 取 id（7c07173）。**实机验证**：已装应用 0.2.1 → 0.2.2 经真实更新通道自动升级完成（下载→passive 安装→重启，无文件占用弹窗，prod 门禁用效——ready 行不再宣称 /v1/dev 免鉴权）。
- **发布工具根治**(2026-08-17)：release.mjs 资产上传从 curl/schannel 改 `gh release upload --clobber`（gh 走 rustls）——根治 Windows 大文件上传握手卡死（0.2.2 发布实证：curl 代理/直连三步全卡 0 字节，gh 同刻传 26MB/40MB 秒级成功，76294d8）。删除 curl 路由轮换/ghToken/deleteAssetsByName，保留草稿幂等续传语义；冒烟验证通过（草稿建→传→删）。
- **已随 v0.2.2 发布**:`eb041b7` 更新安装前杀 sidecar 消 NSIS 文件占用弹窗、账本 promise 校验可用性（arc 缺省落 planted、非法值报枚举与条目 id、工具描述补全四维 entry 结构）；审计收口——workDir 清洗（拒控制字符 + 存在性/目录校验，堵目录名注入系统提示）、prompt/skill mtime 热重载（0008「改文件即生效」补齐，不再靠重启 core)、阈值数字单源化（搜索 limit 20 / excerpt 30 / 问题日志尾 40 行 / 工具结果 500)、/v1/dev prod 门禁（esbuild bundle 注入 `__CORE_COMMIT__` 判定，prod 回 404)、release.mjs 两阶段上传重试 + 工作树预检 + 版本落账自动化（commit/tag/push，幂等）、e2e-workflow 写作闭环剧本（起草→暂存→采纳→落章→快照→冷读，7 步）。审计条目「dev 裸跑 domain 默认 npx 命令 Windows 不可用」经实证推翻（MCP SDK 走 cross-spawn,npx.cmd 可正常拉起），条目移除。
- **批三规划定稿**(2026-08-17，决策 0009)：理解层现状四方取证（亲读 ledger.ts 全文 + 三路深挖），判断为「表达力/闭环/内联」三层欠账；批三切三小批——闭环接通 → 账本表达力 + 表现形式 → 内联注入；skill 预置无硬顶改书级试跑转正制；写作 skill 调研落档 `docs/archive/写作skill调研-2026-08-14.md`。
- **角色与方案定稿**(2026-08-17，决策 0010)：提示词只开放模板级（角色/风格），两层结构——角色自由组建（app 预置 + 书级 .novel/personas/ 遮蔽）→ 方案 = 角色×通道分配表（书级 .novel/schemes/ 多套，激活指针 .novel/active-scheme 随书走）；注入面分契约/姿态/数据三层；确定性检查不被方案配置；讨论陪练从 skill 候选转角色预置；壳入口定 Toolbar 方案 pill。
- **批三-1 闭环接通 已完成**(2026-08-17):domain 加 issue_append/issue_set_status 工具（22→24;CR 行加 status 列，countBlockers 只数未处置）;/v1/review 确定性落盘 issues.md（不靠模型自觉，降级不阻断返回）；壳 ReviewPanel 处置闭环（已处理/已知 → 标灰 + 本地计数）；注入面校订（cold-read.md 删机制上不可执行的残留指令、JSON 契约化，severity 统一四级 + category,chat.md 补工作流指引，兜底串同步）；账本渲染正文加「勿手改」警告；审计清零（裸联调 token localStorage 记忆、CORS 白名单反射收窄——localhost/127.0.0.1/Tauri 三源、壳侧目录约定收 paths.ts)。真实链路实证：review 落盘 CR-001/002 → set_status done → 诊断 blockerCount 1→0。
- **批三-2 账本表达力 已完成**(2026-08-17):结构深化——伏笔加 expectedVolume（预计回收卷）与 links（{props, characters} 跨维引用）；知情维 knows/doesNotKnow 升级 {fact, since?, refs?} 时间轴，YAML 向后兼容（纯 fact 项序列化回字符串，旧账本零 diff)。渲染视图重写——伏笔按卷/状态分层（HOT 前置、resolved/failed 沉底）+〔悬空〕〔逾期·已过 N 章〕〔预计回收卷 X 已过〕标记 + links 内联；知情「（自 章）（伏笔： id)」时间轴化；clock/托管链按章序排序；writeLedger 与 ledgerSlice 同口径带全书章序（basename 作 title 不读正文）;frontmatter 权威态不变。诊断补三条——clock-regression 时钟跨章倒退（MAJOR，中文/阿拉伯数字，同 thread 内比较）、custody-chain-break 托管链断裂（末端持有者矛盾 MAJOR + 引用不存在章 MODERATE)、knowledge-no-knower 保密无知情人（MODERATE/CANON);BLOCKER 复核结论：倒叙叙事与同章正常转手均有误报面，确定性诊断维持不产 BLOCKER。review 结构化输出——streamText + 剥围栏 hack 改为 generateText + Output.array(AI SDK v7 已废弃 generateObject);Output.array 线格式实为 {"elements":[...]} 包装对象，review.md/cold-read.md/兜底串三处同步校订；createOpenAICompatible 两处开 supportsStructuredOutputs（实证修复：不宣称时 SDK 剥掉 responseFormat，真实 provider 直连 502)。壳 LedgerView/knowledgeLine 同步新结构。真实链路实证：临时作品新结构 upsert → 诊断 5 条命中无误报 → 落盘渲染分层/时间轴/内联引用 → 冷读切片同渲染；e2e 7/7（真实 LLM review 出 3 条 findings)。
- **批三-3 内联注入 已完成**(2026-08-17):domain 加 `ledger_chapter_slice`（按章过滤的结构化账本+渲染切片；纯函数 filterLedgerForChapter:all-future 才删/留存活多节伏笔，托管链裁到当前章，clock 跨度行含当前章即留，知情按 since 裁）与 `write_meta`(.novel/ 根下元数据写入：拒覆写可解析为账本的目标，快照+原子写）,24→26 工具；顺带修章序 bug(chapterOrderForWork/diagnostics 从路径字典序改编号感知，≥10 章时「第10章」不再排到「第2章」前，渲染/切片/诊断同口径）。core 注入面按 0010 分层落位（契约→姿态占位→数据）:chat body 加 `chapter` 字段（壳把挂载章 frontmatter id 解析成 relPath 带出，core 不懂 id)，系统提示数据层加「声口摘要」(.novel/style.md 的 `## 摘要` 节，mtime 热重载，1500 字符截断）与「本章账本切片」（服务端调 ledger_chapter_slice，失败降级不阻断）;rewrite 加可选 workDir 注声口（壳三处 rewrite 调用全带上）;normalizeWorkDir 单源化（workdir.ts)。壳：上下文栏随章联动（本章切片/全书双口径+切章自动刷新+AI 调 ledger_upsert 后自动重拉+口径标签跟随实际数据源），采纳联动（adopt 落定自动跑 ledger_diagnostics，有发现才弹轻提示「诊断现存 N 条…让 AI 同步账本」，无发现静默）。候选 skill 试跑架 `core/prompts/candidates/`(scanSkills flat 不扫子目录，不进预置）：声口建档（六透镜+证据回指，write_meta 落 .novel/style.md)、章节起草（纯流程：确认目标→拉切片→审批闸门落盘→同步账本）。真实链路实证 22/22(12 章临时作品：切片过滤全对；chat 注入后模型答出过滤态托管链末节持有者——证明注的是切片不是全账本）。
- **新阶段确立**(2026-08-17，决策 0011):「小说领域 Cursor」全景评审 + 对抗性回顾后定稿——能力对齐与打磨，双轨三批（显性手感 / 理解层供料 / 状态登记）。对抗性回顾裁定：语义索引、会话压缩、ghost-text、全书状态面板、workDir 白名单转触发式挂起；章摘要走导生缓存（否登记型事件流水）;ghost-text 起步被否（AI 味渗入红线），先做触发式续写；行为数据挖掘为第 0 步（注：当晚已由作者访谈替代，见待办第 0 步）;批三-4 收编入批一。
- **铁律回归批 已完成**(2026-08-17，决策 0012)：chat 正文一律经 core 本地工具 `stage_chapter_proposal` 进暂存区（候选模型加 kind=replace/append/replace_all，旧库迁移兼容）——修掉真实写作暴露的 drift：AI 讨论同意后直写 write_chapter 绕过暂存区、设定被写进 manuscript 散章。write_chapter 降级为作者明确指令直写时才用（审批门保留）；chat.md 路由设定→ledger_upsert、书级元数据→write_meta、严禁写 manuscript；write_chapter/create_chapter 描述加反向边界；壳采纳按 kind 分派（追加/整章替换/锚定替换）、append 不打内联装饰、暂存卡带 kind 徽标、流式后自动重拉暂存区。**验证**：全量 581 绿 + cargo + e2e 7/7 + 实模型三探针（续写→stage 落卡且章文件字节不变；设定→ledger_upsert 不建散章；append API 放行）。新章草稿候选缓做。
- **吸收方案定稿**(2026-08-20，决策 0013)：外部并行路线《长篇小说工作流-人机碰撞版》评审收口——设计自洽但零实战、闸门无强制力，冻结为方法论规格库；六项前置决策定稿（裁决留痕 `editorial_notes/decisions.md` / 章 frontmatter 加 `blueprint` 状态位 / 证据锚 `evidence:{chapter,line,quote}` 分批铺开 / 摘要机检字段冻结 / 方案只管通道×角色 / 批三角色状态进账本原则）；壳白名单 4 处；回放预算提级至碰撞之前；首个真实验证载体=B 作品库《我的小说》在本仓立项开写。
- **批一① 角色与方案 已完成**(2026-08-20，决策 0010/0013 落地)：角色库（app 预置 `core/prompts/personas/` 4 个 + 书级 `.novel/personas/` 同名遮蔽；frontmatter kind 缺失宽容、错配跳过——对齐 domain 口径，防书级手写方案激活成功却不进清单的静默失效）+ 方案库（`schemes/` 同构；frontmatter 通道×角色分配表，正文为人读备注永不注入）+ 激活指针 `.novel/active-scheme` 随书——读在 core（新端点 `GET /v1/posture` 返回 personas/schemes/activeScheme)、写走 domain 新工具 `scheme_set_active`(26→27：原子写、方案名取 app∪书级并集校验、未命中报错列可用、空串回默认）。三通道 chat/rewrite/review 请求体加可选 persona,core 在姿态层（契约后、数据前）拼「## 当前角色」，按名找不到 warn 零注入；cold-read.md 身份句降级为默认角色注释；personas/schemes 随包首启释放（子目录补缺）。壳：Toolbar 方案 pill(tb-mode 胶囊+WorkMenu 式下拉，恒可见，无激活显示「方案：默认」)+ scheme store(boot/换书重拉、失败静默降级）+ 三通道按激活方案 channels 映射带 persona。protocol 1→2(0007 同步并补 review 端点行）；两条预置 skill description 改触发条件式。预置：责编（**瘦身版**=有据·有对立·有后果+定位/对抗/裁决三层+「我不知道」合法；结构镜面/节奏诊断剥给机械层与批二数据层，五层输出流程完整留给批一③碰撞提示词——作者短篇试跑认定五层版责编负担过重、讨论空转）、讨论陪练（苏格拉底）、毒舌书评人、小白读者（第一读者视角+体验攻击面并入）；方案 结构对抗型（chat/review→责编）/体验优先型（chat/review→小白读者，B editor-schemes 改写）,rewrite 通道刻意留白（写作主链路不上姿态，防 AI 味）。**验证**：全量 623 绿（core 167/domain 240/shell 216)+ cargo 16 + check 0 错 + 双路实跑（posture 端点无/带 workDir、scheme_set_active 激活/指针落盘/报错列可用/清除、真实模型 chat 带责编出三层结构+举证纪律降级+空库声明证据不足不硬裁；书级遮蔽与无 kind 手写方案识别）。
- **批一② 工具回放预算校调 已完成**(2026-08-20)：落库与回放分离——落库全量（单条结果 500→20_000 防爆，超才截）；回放按工具分档（内容类 read_chapter/read_snapshot/ledger_read/ledger_slice/ledger_chapter_slice/search_content/skill_read 给 3000，其余 500），回放总预算 25_000→40_000；截断末尾加「…（已截断，可再调工具取全量）」标注。常量单源仍在 chat.ts；旧库行兼容（旧 500 截断行原样回放）。**验证**:check 0 错 + 全量 625 绿（core 169,chat 新增分档/落库用例 4)+ 实跑（真实模型 read_chapter 结果 2453 字符全量落库；追问位于第 604 字符处的暗号，第二轮零工具调用直接答出——旧 500 档必截）。未动 rust,cargo 未跑。
- **批一③ 碰撞模式 已完成**(2026-08-21，决策 0013 落地)：chat 请求体加 `mode:'collide'`(protocol 2→3，壳 App.svelte 与 src-tauri EXPECTED_PROTOCOL 双处同步，握手测试同步）；core 新封存层 `prompts/collide.md`(碰撞协议：有据·有对立·有后果 + **方案→漏洞→反方→裁决**四节固定输出 + 裁决登记义务 + 轻碰撞入口；与瘦身责编不双份），契约层注入；数据层加「## 讨论沉淀（最近裁决）」——服务端调 decision_tail（章过滤+尾部 20 行），预算 1500 截断标注，仅 collide 且有 workDir 才注（非碰撞零注入零调用）。domain 纯加法 27→30:`decision_append`（裁决留痕 `editorial_notes/decisions.md`，一行一条 `- D-NNN | 日期 | 议题 | 立场 | 裁决 | 理由 | 章`，追加式，推翻引原编号）、`decision_tail`（只读尾部摘要，文件缺省降级空）、`chapter_set_blueprint`(fm 单键最小侵入改/增/删，none=删键，快照+原子写）;FM_KEYS 加 `blueprint`（宽容解析）,buildChapter 透出。壳：composer 碰撞胶囊（复用 tier pill 形态，localStorage 持久化；开时带 mode，方案未指派 chat 角色则 fallback 讨论陪练——壳策略 core 不感知）+ 四节对比色渲染（`collide-parse.ts` 容错：四标题齐备且按序才分段着色，否则整泡普通渲染；方案/裁决=pro·accent 系，漏洞/反方=con 系，theme.ts 派生 token)+ 结构树 blueprint 徽标（locked 实心点/draft 空心圈，复用 status 渲染位）。e2e-workflow 扩 7→9 步：碰撞（断四节标题齐备）→ decision_append+置 locked+读回核对 → 起草，闸门进自动化（0013 验证节）。**审核实录**：子代理三分支报绿后复核 diff，补三处漏改（protocol 壳侧双期望 2→3、rust 握手测试断言）+风格统一（沉淀节括号全宽、ChatColumn 双解析改 @const)。**验证**:check 0 错 + 全量 666 绿（core 177/domain 259/shell 230)+ cargo 16 绿 + e2e 9/9（真实 LLM 碰撞四节齐备、留痕/闸门/透出读回全对）。
- **审计修复批 已完成**(2026-08-21)：批一③后做五路取证式综合审查（不看文档看实现；运行级实锤+逐条复核），结论=基础设施健壮（鉴权/CORS/token/SQL 注入/原子写/回放预算全有测试看守，666 数字属实），但暴露两个模式性弱点——**护栏边界一致性无人收口**（同仓两种路径守卫口径并存）与**状态机后半生缺看护**。修复：① P0 domain 路径守卫统一收口——新单一原语 `resolveInsidePosix`(fsutil.ts：先 resolveInside 消解 ..、再对归一化 posix 判前缀，附 symlink 逃逸 realpath 落点校验）,delete/rename/move 章卷六工具+read_snapshot+word_count 全部切入（实锤：修复前 `manuscript/../.novel/ledger.md` 能把账本软删，修复后拒绝且目标不动）;② P1 壳 rust——core Ready 后退出也置 Failed（不再僵尸分发死端口）、握手拒接杀已拉起 core、ready 60s 看门狗、kill 收敛为 kill_core_tree(Windows taskkill /T /F 连 domain 孙进程）;③ P1 版本单源五处→**六处**(package-lock.json workspaces 段曾全部漂移 0.1.0↔0.2.3，已修正并入 check-versions/syncVersions/commitAndPush;release 预检白名单版本文件解决半途重跑自锁，Cargo.lock app 段同步消 --skip-build 旧版）;④ P2 core——同 session 并发 409 在飞锁（回放交叉污染）、candidate 状态机迁移校验（壳实际只用 pending→adopted/discarded，其余非法迁移 400;adopted/discarded 终态）;⑤ P2 壳——流式切会话代际守卫（streamGen+abandonInFlightStream，旧流回调整体失效）、openChapter/saveCurrent 竞态（openSeq 陈旧丢弃+保存快照 cur+saving 重入不吞脏存）、notes flush 清脏加内容快照比对、adopt 部分失败收集继续+finally 必重拉、core 死亡网络层错误提示可行动化（CoreNetworkError);⑥ P3——413 连接 Connection:close+destroy、孤儿守护退出走完整 shutdown、prompt/style 热重载缓存加 size 双因子、rewrite 比率护栏短选区（<20 字）豁免、streamingIdx 改 store 持有（不再误标错误气泡）、localStorage 写补 try。**审核实录**：五 coder 报绿后主 agent 复核 diff 全看+实跑复现（绕过实锤转拒绝）;D/E 两 coder 曾超时被断，恢复后续跑收尾。**验证**:check 0 错（六处一致）+ 全量 692 绿（core 182/domain 268/shell 242)+ cargo 17 绿 + e2e 9/9 回归（断言加固：留痕步不再硬断言 D-001——实跑观测到模型在碰撞步已按协议义务自调 decision_append 先登记，改按实际返回 id 核对；模型自觉执行裁决登记义务获实证）。in-turn 单工具结果不设上限（观察项转结论：0012 后模型读整章再 stage 提案是主链路，单章全文是正当检索面，上限只打在跨轮回放）。
- **MSI 打包通道弃用 已完成**(2026-08-21):`tauri build` 默认 targets=all 打 MSI 必挂——`core/prompts/` 中文目录名（责编/讨论陪练等）超出 WiX 1252 码页（LGHT0311，实跑复现）；定为单 NSIS 通道——tauri.conf.json `bundle.targets` 收 `["nsis"]`,release.mjs 移除 MSI 收集/告警（本就容忍缺失），README 发布说明同步（顺手改正「五处版本号」陈旧表述为六处+lock)。**验证**：默认 `npx tauri build --ci` 全程通过，产 NSIS+sig（此前两次默认构建均挂在 light.exe)。
- 验证基线：`npm run check` 0 错（含 check-versions 六处一致）、`npm test` 全绿 692(core 182 / domain 268 / shell 242)、`cargo test` 17 绿、`node core/scripts/e2e-workflow.mjs` 9/9（真实 LLM,key 在场才跑）。

## 规范（本轮起生效）

- 版本号：六处事实源（tauri.conf.json / Cargo.toml / core·domain·shell package.json / package-lock.json 的 workspaces 三段；Cargo.lock 的 app 段顺带同步）,core/domain 代码读 package.json;`npm run check` 内含 `check-versions.mjs`,release.mjs 写完自检，漂移即失败。
- 提交：conventional commits 中文摘要；一切 git 写操作（commit/push/release）须作者明确指令。
- 文档：活跃件只留 `01-产品定义.md`、本件、`decisions/`；历史入 `docs/archive/` 且不改写；新决策按序号入 `decisions/`。
- prompt/skill：文件机制见 0008；预置 skill 无数量硬顶，书级试跑 + 真实写作验证留住才转正（0009)，候选试跑架 `core/prompts/candidates/`(flat 扫描不触及）；结构级检查（伏笔/时间线/口吻/知情权）永远是 domain 确定性工具的职责，不写成 skill。
- 安全口径：账本/问题日志路径白名单（`.novel/` 根下或 `editorial_notes/` 下 .md);apiKey 打码回传 + 占位即保留；连接 token 不进日志；webview CSP 兜底；workDir 清洗后才许拼系统提示。
- 验证：改完必须 check/test/实跑，无法验证就明说；主 agent 审核不盲信回馈结论；发版前过亲测清单。
- 新阶段结构纪律（0011)：人审内容留 md、机械态/导生态可入 sqlite 或缓存（按「谁会编辑它」分流）；导生物可焚可重建；预处理走「脚本 → 导生物 → 主程序只读」，不拆仓；30 个 MCP 工具契约不动只做加法；新增理解层维度必须带证据锚 + 至少一条确定性诊断 + 注入预算上限。

## 待办

**新阶段：能力对齐与打磨（决策 0011,2026-08-17 起；双轨三批，每批过北极星校验门：成稿更快了吗 / 离谱更少了吗）**

0. **第 0 步 · 行为数据挖掘 → 作者访谈替代**（2026-08-17，0011 修订）：单作者产品问人比猜数据便宜，且采纳留痕样本窗口已被 v0.2.3(0012) 重置。访谈结论：主痛点=AI 思考碰撞不足，驳回主因在讨论/理解质量而非正文质量（生成是讨论下游），不据此上调生成层；保留「诊断误报统计」单项回看（对账器上线后自然积累，届时一次回看）；超预算占比维持挂起。
1. **批一 · 显性手感（2026-08-20 依 0013 再修订）**:① ~~批三-4 角色与方案收尾~~（已完成 2026-08-20，见当前进度「批一①」）;② ~~工具回放预算校调~~（已完成 2026-08-20，见当前进度「批一②」）;③ ~~碰撞模式~~（已完成 2026-08-21，见当前进度「批一③」);④ 触发式续写（快捷键/按钮唤起、便宜档、总开关；不做 ghost-text 起步）。
2. **批二 · 理解层供料**（全导生/确定性）:① 章摘要（采纳时自动生成，导生缓存可焚可重建；tension/scene_type/字数三机检字段落盘即冻结，节奏诊断只读字段不读散文，0013 决策 4）;② 对账器（账本证据锚回验正文，防漂移核心件；碰撞模式地基——碰撞质量依赖 AI 对既定内容的理解，账本可信是上游；schema 准入=0013 决策 3，统一 `evidence:{chapter,line,quote}` 分批铺开）;③ list_trash 收口进 domain（软删记录现由壳 localStorage 跟踪，换机即丢）。碰撞深化形态预判（批一①试跑结论留存）：证据包先行（机械/数据层供镜面与节奏）为主、三方会审为后（重大分歧用，独立上下文防同上下文假三方）。
3. **批三 · 状态登记**（触发式：对账数据或真实写作显示术语/角色状态失控时启动）：角色卡一等实体（境界/状态/阵营/生死/位置）+ 设定登记（术语一致性，复用登记表模式）。
4. **亲测清单**:① CSP 与 prompts 首启释放已随 0.2.1 发出，日常使用中留意；② prompt 热重载与 /v1/dev prod 门禁已随 0.2.2 发出并实机随更新生效（ready 行不再宣称免鉴权），日常使用中留意；③ updater 文件占用弹窗修复已随 0.2.2 发出，0.2.1→0.2.2 实机升级无弹窗（本次发起更新的是旧版，属预期之外的好结果）,0.2.2→后续版本更新时继续留意；④ chat 正文暂存提案与设定路由（决策 0012）已随本批落地，日常写作中留意——正文是否进暂存区、设定是否走 ledger 而不建散章；本地工具 stage_chapter_proposal 的表现（工具卡/暂存卡）留意。⑤ 批一-1 角色与方案：日常写作中留意方案 pill 切换后三通道姿态是否真实变化（chat 讨论、改写、贵档审阅）、personas/schemes 改文件即生效（热重载口径同 skill）、书级遮蔽是否符合直觉。⑥ 批一③ 碰撞模式：日常碰撞中留意四节输出遵循率（壳解析失败=整泡普通渲染，即失败信号；失败率超阈值升 Output.array,0013)、裁决登记与 blueprint 置位是否被模型自觉执行、讨论沉淀注入是否供料有效。
5. **缓做**（触发条件驱动）:skill 管理 UI（触发：skill 多到管不过来）、promptfoo 回归（触发：prompt 迭代需防回归）、角色/方案壳内编辑 UI（触发：改文件改烦）、确定性治理可调化（触发：真实误报/漏报；方向：书级抑制阀→词表数据化→规则注册表）、read_meta / style.md 全文可读回（触发：声口重建需吸收作者手改；当前重建即覆盖，快照在 .novel/history/ 兜底）、语义索引/embedding（维持 0004/0006/0009 后置结论；触发：真实查询失败被记录）、会话压缩摘要层（触发：第 0 步实测超预算会话占比超标）、ghost-text 行内续写（触发：触发式续写留住率达标）、全书状态面板（触发：状态数据源 ≥3 且散在多处）、workDir 根白名单/调用一致性拦截（触发：导入外部文本或多作品并发）。

## 明确不做

图形化世界观、多人协作、模型市场、EPUB 排版、AI 绘图、治理体系、跨卷移动、壳内指令模板搬家（0009 记档）、预处理操作台（工程概念不泄漏给作者，0011)、全书自动建知识图谱（0011)、游戏式单一权威实时状态机（0011)。

## 决策速查(`decisions/`)

做判断前先查这里，每条一句：
- 0001 harness 选型：Vercel AI SDK v7，不手写 agent 内核
- 0002 接线：Node sidecar + HTTP/SSE，不过 Tauri IPC
- 0003 壳基线：Tauri 2 + Svelte 5 + TipTap（布局快照已演进为 v5 五栏，见正文标注）
- 0004 数据模型与领域服务：domain MCP（结构即智能的载体）
- 0005 暂存区与讨论存区：归属 core sqlite
- 0006 修复轮：周 2-3 审核后质量修复（「updater 占位」已被真实通道取代，见正文标注）
- 0007 协议契约 v1:/v1/ 版本化 + 进程握手 + event_pump
- 0008 提示词与 skill 统一文件机制：md 文件单一事实源，改文件即生效（热重载已落地）
- 0009 理解层闭环与内在深化路线：批三三批（闭环 → 表达力+表现形式 → 内联）;skill 预置无硬顶、书级试跑转正（批三-1/-2/-3 已完成；批三-4 收编入 0011 批一）
- 0010 角色与方案两层姿态注入：提示词切封存/开放层，方案=角色×通道分配表（书级多套+指针随书）;注入面分契约/姿态/数据三层；确定性检查不被方案配置（批三-4 收编入 0011 批一执行）
- 0011 新阶段：能力对齐与打磨——双轨三批；北极星双指标校验门（成稿速度/离谱率）;行为数据挖掘已由作者访谈替代（2026-08-17，保留诊断误报单项回看）;批一新增碰撞模式（对话式思考碰撞，0010 收尾后首位，零新 UI）;对账器升级为碰撞地基；挂起清单触发驱动
- 0012 铁律回归：chat 正文经本地工具 stage_chapter_proposal 进暂存区（候选 kind=replace/append/replace_all，采纳才落盘）;write_chapter 仅作者明确直写时用（审批门保留）;设定→ledger_upsert、书级元数据→write_meta，严禁写 manuscript
- 0013 吸收人机碰撞方法论：外部路线 B 冻结为方法论库；裁决留痕 `editorial_notes/decisions.md`；章 frontmatter 加 `blueprint` 状态位；证据锚统一 `evidence:{chapter,line,quote}`；摘要机检字段冻结；方案只管通道×角色；壳白名单 4 处；回放预算提级至碰撞之前

## 文档地图

- 活跃：`01-产品定义.md`、本件、`decisions/`。
- 历史：`docs/archive/`（诊断 00 / roadmap / 壳重设计系列 / v4 / v5 / 下一步方案 / 审核手册 / 生态调研 / 路线偏移与最新进展 / 写作skill调研-2026-08-14）；逐件一句索引见 `docs/archive/README.md`。
