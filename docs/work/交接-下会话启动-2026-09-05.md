---
status: active
batch: 交接件
produced: 2026-09-05
---

# 交接件 · 下会话启动（2026-09-05 深夜，v0.2.13 发版会话末）

> 单一事实源仍是 `docs/current/现状.md`；本件只做「上会话做了什么/下一步从哪续」的启动加速。读完本件后按需读现状.md 对应节，不全量加载。

## 一句话现状

v0.2.13 已发布（块 4.0→4.2 三批随版）；下一步=4.3 角色卡批 + 0018 主体实施（并行预处理独立线）开工；**裁决负担数据（全块扩容闸门）挂 4.3 真人小段时点，需作者参与**。

## 上会话完成了什么（按 commit 可溯）

1. **0017/0018 审定流水线**：多角度审查（P1×1/P2×6 落实）→ 总设计三调整（4.0+ 对照前置/4.1 瘦身/超域+坐标两规约回灌）→ 作者审定 → 改写为 `docs/reference/05-账本与长篇连续性.md`、`06-完本书分层导入与事件骨架.md`，草案原件归档。
2. **4.0+ 对照实验**（`docs/work/块4对照实验-4.0加-2026-09-05.md`，随 v0.2.13 归档）：分层 vs 均匀（383 calls）；作者裁决「有条件通过」→ 召回补测兑现（`scripts/bench/recall-recheck.mjs`：选区内 27.9% vs 32.8% 过门限；区间投影未选区 16.6%=0017 区间语义首证）→ **0018 主体实施终裁通过**（与 4.3 并行预处理独立线）。
3. **4.1 地基批**（三刀）：`domain/src/envelope.ts`（信封+区间代数+双读零 diff，真账本 2429 条目 66ms 实测）+ `domain/src/proposal.ts`（提案操作序列/protect 写闸/裁决状态机/md round-trip）+ `domain/src/ledger-index.ts`（区间裁剪+类型配额+名字倒排：真账本切片 44-49 万→29,999 字符 12ms）+ `ledger_slice` 加 budget 闸（缺省零变）。
4. **4.2 薄切片裁决回路批**（承诺·伏笔窄域全环）：`domain/src/promise-prefilter.ts`（确定性预筛+超域候选）+ `domain/src/inbox.ts`（收件箱 md 存储：append 幂等/adopt 落账+回读验证/discard 理由枚举+卷锚）+ MCP 五工具 + `core/src/scan.ts`（/v1/scan/promise，便宜档判定→提案入箱）+ 壳收件箱面板（左栏第三 tab + Toolbar 徽章 + 批量裁决）。

## 下一步从哪续

- **4.3 角色卡批**（下一主线批）：信封原生角色类型——静态层登记型（直写+归一化诊断+注入预算+壳角色面板+人名字段可解析引用）+ 动态层接裁决回路；规范=`docs/reference/05` §角色维；别名归一理想标定场=诡秘 1735 对嫌疑（name-census 报告）。
- **0018 主体实施**（并行独立线，scripts 侧纯导生）：L0-L3 分层管线工程化、覆盖率台账落盘形态、L3 共享读段缓存粒度、角色动态缺口随首次全量灌装跑齐；基建=`scripts/bench/layered-pipeline.mjs` 现成。
- **裁决负担数据**（4.2 出口，全块扩容闸门）：挂 4.3 真人小段——**需作者参与**，不可单方面推进。
- **块 3 主体**（模型方案内绑定）在块 4 之后，未动。

## 环境与坑（本会话实测）

- **子代理通道不可用**：general-purpose/Explore/ox-dmx 均报 `Model provider is not configured: builtin:zai-start-plan`——审查类工作由主代理亲读替代（0905 实录见 archive 0017/0018 原件修订注）。
- **LLM 通道**：opencode zen（deepseek-v4-flash）**余额不足**、nemotron 连接超时；可用通道=OpenRouter `minimax/minimax-m3:free`（试车台同款，`LLM_BASE_URL=https://openrouter.ai/api/v1 LLM_API_KEY=$OPENROUTER_API_KEY`）。e2e-workflow 因通道余额本会话未跑（发版披露）。
- **免费档 LLM 输出坑**：长 JSON 字符串值内未转义引号——`scripts/bench/lib.mjs` 已有 `repairInnerQuotes` 状态机修复，新管线直接复用。
- **Windows ESM 直调 domain TS**：node -e 里 `C:/` 路径 import 会 ERR_UNSUPPORTED_ESM_URL_SCHEME——用 `pathToFileURL().href` 或落在 `scripts/bench/*.mjs`（tsx register 相对导入）。
- GitNexus 已接入（AGENTS.md/CLAUDE.md/.claude/skills，作者裁决保留；check-docs 的 50 行检查豁免生成块）。编辑代码前按其纪律跑 impact；本次三批全为纯加法新模块，冲击面零。

## 纪律提醒（等作者/触发器）

- 一切 git 写操作（push/release）须作者指令——本会话 push 与发版均经作者明示。
- 4.2 出口「裁决负担实证不成立→4.3/4.4 降规格或停」是 0017 §9 重开权的一部分，数据落地即触发判断。
- 北极星复测锚：盖真章续写基线 1.8/2.4/2.2（测量报告 §5），块 4 注入成型后同点位复测。
