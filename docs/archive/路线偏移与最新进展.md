# 路线偏移与最新进展

> 性质:第三次出发路线的偏移说明与现状快照,配套 `docs/archive/roadmap.md`。
> 因实际实现与原路线差异较大,另起本文记录;不改动既有文档(仅 roadmap 顶部加指引)。
> 更新:2026-08-16
> 文档归整(2026-08-16):历史文档入 `docs/archive/`(诊断 00/roadmap/壳重设计系列/v4/v5/下一步方案/审核手册/生态调研),活跃件只留本件 + `01-产品定义.md` + `decisions/`。

## 一、路线偏移总览

原 `archive/roadmap.md` 第 4 周「理解层」规划:从 novel-rl 搬 understanding 层(约 5,700 行),
只接一个出口——一致性审阅清单(伏笔 / 时间线 / 口吻 / 知情权)。

实际走向「账本重做」:不搬旧代码,参考考古报告 + fiction-forge 冷读协议,
重做「四维账本 + 双层审阅」。原因:novel-rl 的 understanding 层绑定旧架构
(数据通路 / EntityStore / agent-kernel / outbox),重做不参考(见 WS-3 决策与 T2 考古)。

同时,第 2/3 周的「壳」在作者实测后触发整体重做(v3:窄条 + 多栏),
详见 `docs/archive/壳重设计-*` 系列。

## 二、本次任务实际内容

两条主线并行,均已实现并收口:

### 线 1 · 壳重做(父:壳实现系列)

- WS-5:高保真原型(v2 窄条 → v3 两栏到多栏切换)
- WS-10:D 组工程通道——`/v1/` 协议版本化 + 进程握手校验 + event_pump(D3 依赖冻结裁定不做)
- WS-11:A 组 domain 工具——新建章/卷、重命名、卷内拖拽重排(事务化重编号)+ frontmatter 模板
- WS-12:壳实现——v3 窄条 + 多栏、B1/B3–B10 能力、设置面板;卷/章编号规范改阿拉伯数字
- WS-21:作者实测 5 项缺陷修复(新建卷落盘 / 流式改写 / 固定切换栏 / 拖拽 / 暂存全览)+ 卷排序 + 卷拖拽返工

### 线 2 · 审阅账本(父:规划、头脑风暴与研究)

- 零件 1(WS-7):LAY 量化指标扫描器(domain 确定性检查,零 LLM 成本)
- 零件 2(WS-9):fiction-forge 冷读协议试跑(账本式 vs 无状态 lint 对比)
- 零件 3(WS-8):qidian-mcp 取回评估——结论不整套接入,改移动端 SSR 路径
- T1(WS-14):合入第 1 步零件到 main
- T2(WS-15):考古提取旧 understanding 层规则 / 提示词 / 校验逻辑 / 金标准 + 映射建议
- T3(WS-16):移动端 SSR 采集脚本 + 3 章真实语料(7540 字)
- T4(WS-17):四维账本(时钟表 / 道具托管 / 承诺登记 / 知情地图)+ 双层审阅(冷读贵档 + 扫描器便宜档)+ BLOCKER 清零提示

## 三、现状

### 已合入 main(`9bb6336`)

- 壳重做 v3(窄条 + 多栏 + B 组能力 + 设置面板)、`/v1/` 协议版本化、LAY 扫描器、冷读记录、A 组 domain 工具、卷/章编号阿拉伯规范;
- 测试:`npm run check` 0 错 0 警、`npm test` 303 全绿(core 74 + domain 148 + shell 81)、`cargo test` 3 绿;
- domain 20 个 MCP 工具。

### WS-17 账本已合入 main(2026-08-14,merge 0d14182)

- 四维账本(ledger)工具 + 双层审阅 + BLOCKER 清零提示;
- 合入时独立二审打回 3 个严重项(ledgerPath 无白名单可覆盖正文、ledger_slice 可注入任意文件、坏 YAML 静默清空账本),修复后复验通过(34cd7bd);
- 待补:账本/扫描诊断在壳内无消费者(BLOCKER 标红与清零出口未接壳),见「未来发展方向」。

### 流程

- 已接入 squad 路由。独立二审机制 2026-08-16 作者取消,`审核手册.md` 随之归档;完成标准回落为 AGENTS.md 原样:改完必须验证(测试/构建/实跑),主 agent 审核不盲信回馈结论。

### 安全修复批(2026-08-16,待随 0.2.1 发布)

- 全仓硬编码审计(5 路并行)后修:CSP 兜底(原 `csp: null`)、core ready 行 token 不再进日志、apiKey 打码回传+「占位即保留」、账本/问题日志路径守卫白名单化(`ledgerAbs`/`ledger_slice`/`diagnosticsForWork` 统一口径,`.novel/` 根下或 `editorial_notes/` 下);
- 版本号收敛为 5 处事实源(package.json×3 + tauri.conf.json + Cargo.toml,core/domain 代码内常量改读 package.json),新增 `scripts/check-versions.mjs` 挂进 `npm run check` 与 release 自检;
- 验证:二审独立复跑全绿(check / 440 测试 / cargo 16 + 对抗性路径实测);CSP 运行时效果留作者亲测。

### 提示词与 skill 统一文件机制(决策 0008,2026-08-16,待随 0.3.0 发布)

- 机制:prompt/skill 同为 md+frontmatter 文件,单一事实源 `core/prompts/`;prod 首启释放到 app 数据目录(缺才拷、不覆盖),改文件即生效不吃发版;core 三处 SYSTEM_PROMPT 与冷读契约全部改从文件加载(坏文件回退兜底);skill 清单注入 chat 系统提示,正文按需经新工具 `skill_read` 拉取,书级 `.novel/skills/` 同名遮蔽 app 级;
- 预置 skill 两个:去AI味润色(原创,绕开 renwei 许可)+ 章节体检(接线 scan_quality/ledger_diagnostics/冷读);
- 验证:二审真实链路 e2e 通过(释放/注入/skill_read/遮蔽/不覆盖/坏文件兜底),check / 456 测试 / cargo 16 全绿;
- 留待后续:真实安装包端测首启释放链路(发布前亲测);dev 裸跑 domain 默认 `npx` 命令在 Windows 不可用(既有问题,另开小项);mcp.ts env 整份透传可收紧白名单(非必须)。

### 发布通道(2026-08-14)

- 壳 updater 从占位端点切换为真实通道:`https://github.com/sanjiao-orl/final/releases/latest/download/latest.json`;新增 `scripts/release.mjs` 零依赖发布脚本(同步三处版本号 → 签名构建 → 生成 latest.json → `gh release` 上传),见 README「更新通道」。
- 首发 v0.1.0 与 v0.1.1 已通过真实 release 流程发布,并实测 0.1.0 实例的 updater 检查/下载/安装链路。

## 四、未来发展方向

### 三批规划(2026-08-16 定,批一/批二已收口见上)

- 顺序:**批一 修复 → 批二 skill 机制落地 → 批三 内在深化(主线)**。理由:修复批小且挡发布;skill 机制顺承接存量 prompt 文件化;内在深化无终局、按写作反馈切批、每批以作者亲写一章收尾。
- 批三(内在深化)两个锚点:① 审阅闭环真实写作验收(BLOCKER 标红→清零→冷读在真实章节上过一遍);② 数据模型重建收尾。pause 状态,等作者指令。
- 审计遗留风险(2026-08-16 全仓审计发现,**未修**,按优先级):
  1. CORS `*` + `/v1/health`、`/v1/dev` 免鉴权,dev 联调 token 走 URL 查询串(http.ts:5、lib/core.ts:253-258)——本地低危;
  2. `chat.ts:44` 壳传入 workDir 未清洗直拼系统提示——提示词注入面,本地低危;
  3. dev 裸跑 domain 默认 `npx` 命令 Windows 不可用(config.ts:176,prod 不受影响);
  4. release.mjs 不校验工作树/不打本地 tag/不 push——GitHub tag 与注入 commit 可能错位;
  5. 漂移类:审批去重键与 DANGEROUS_TOOLS 名单壳内多处散落;`manuscript/`、`.novel/*` 目录约定无单一常量源;阈值数字(limit=20、excerpt 30、尾 40 行、500 截断)双写;
  6. MCP_DOMAIN_CMD 缺省指源码路径(仅 core 独立启动无 env 时踩)。

- 北极星(不变):作者用本软件写完 3 章可投稿水准的章节;以真实写作验收,不以测试绿为验收。
- 待办:账本/扫描的壳内审阅出口(一键诊断 + BLOCKER 标红 + 清零提示),WS-17 最后一公里。
- 遗留功能:B2 上下文面板(entities / 故事圣经)——账本落地后可重新评估(并入账本或单独做)。
- 长期验收:roadmap 止损线(4–6 周无动力写完一章则终止)持续观察。
- 明确不做(不变):图形化世界观、多人协作、模型市场、EPUB 排版、AI 绘图、治理体系、D3 依赖冻结、跨卷移动。
