# 现状件批史归档 · 至 v0.2.14（2026-09-05）

> 2026-09-05 随 v0.2.14 发版按「随版归档」纪律从 `docs/current/现状.md` 移入，冻结不改写。本卷记录 v0.2.13→v0.2.14 期间批次：4.2.1 修复批、4.3 角色卡批、裁决回路加固批（4.4 前置）、4.4 立项。

## 批史详录

- **4.2.1 修复批**（✅ 0905，块4大规模评审后的修复批；提交 ea412b4 评审 / f9929e7 / 11bdd73 / 04e72b0 / df8e87f / caeecfa 收口）：触发=块4新增代码大规模审查（六路子代理深审+主代理亲证 P1 全亲证+基线复核，bugs.md 入帐 19 条）。P1×2：壳扫描链路 30s 死线+静默失败且服务端断连继续烧钱（scanPromise 接 600s 长超时+每章独立死线+AbortSignal.any 随断连中止+壳侧失败接 work.error 红条）；protect 写闸对 remove 全维失明（protectHit 补收顶层 id/character/name/fact 字段）。P2 簇：回读验证逐维对账真实落点（clock 按 chapters 键/登记表按文本精确匹配，修复前删除成功反判❌、clock 恒✅空转）、裁决终态守卫、收件箱白名单/CAS/原子写、误报键永久抑制再入、scan id 改名字散列基+章键补零、单章失败隔离继续批、detail 同序对齐、壳批量裁决逐条 try/catch、第三 tab 互斥单一出口、壳状态 boot 即拉、recall-recheck 判据改同章受限口径（0905 实测复算 −3.1pp 仍过门限，0018 终裁幸存且强化+per-fact 判定落盘 jsonl）、layered-pipeline l3 续跑契约修复（已完成章返 done 不计连击，实跑 120/120 跳过）。
- **4.3 角色卡批**（✅ 0905，角色维全量落地；提交 d735890 / a7d67bc / 0a67126 / 1aec43a / 451716a / fb2de88 收口）：characters 可选表（CharacterEntry name/aliases/kind/role/faction/description/relations/states；键不凭空出现=旧账本零 diff，空表序列化省略）；character-norm.ts 归一化（称谓规则+排除表+编辑距离1+两遍构典）+ matchName + resolveNameRefs 可解析引用 + samePersonVariants；character-prefilter.ts 确定性预筛（词典提及/超域疑似含 minCount 门槛/写法变体）；ledgerDiagnostics 三条新诊断（alias-conflict/state-order/ref-unresolved）；索引配额重分（character 0.15，promise 0.35/prop 0.2/knowledge 0.2/clock 0.1）；MCP 四新工具 42→46（character_upsert 拒收 states=静态直写不改动态、character_list、character_prefilter、character_refs；ledger_upsert 加 assertNoDirectStateWrite 直写闸=states 只能经裁决回路落账，收件箱 adopt 不经此闸）；core /v1/scan/character 零 LLM 补账路由（超域疑似映射 ADD 草稿/写法变体映射 NOOP 观察提案→inbox_append）+ 冷读预算闸接线（review callLedgerSlice 传 budget=30000+注入构成随响应回传）+ 03 契约登记；壳左栏第四 tab 角色面板（静态卡+states 变迁时间线+扫描按钮，boot 即拉，四 tab 互斥）。三路子代理评审修复：[P1] applyOps character upsert 动态层保护（entry 不带 states 保留既有 states——修复前静态字段更新整条覆盖抹掉动态历史）+P2×8（ledger_upsert 直写闸/超域疑似接排除表/minCount 跨章聚合后过滤/verifyTargets 补 character 分支/matchName 两遍构典/NOOP 观察键抑制再入/scan-character detail 回填真 id/shell createFromSelection 清角色 tab）。4.2.1 挂账四条+promptfoo 缺位随批核销。
- **裁决回路加固批**（✅ 0905，4.4 开工前置；提交 e50913e / 60ead10 登记）：触发=收件箱裁决状态机的图级语义审查（真实三态 vs 图示模型核对，发现两条 P3）。修复：全 NOOP 提案同样过 validateProposal（protect 触碰即拦契约不再有快车道豁免——4.4 雷达反向提案多为 NOOP 观察形态，热路径语义先行钉死）；损坏块守卫（解析失败伪条目 ops 空不可裁决抛「提案块损坏」，防快车道无声采纳并覆盖解析失败留痕）；附带修复损坏块头部 id 归因（parseYaml 对裸 id 串返回字符串致 header?.id 恒空，损坏块一律 UNKNOWN 无法按 id 定位）；discard 携带既有 verify（回读❌后改判驳回，留痕不随驳回抹掉）。回归钉 +3，domain 443→446。审计同时产出两条新挂账指 4.4（裁决拒绝 denials 不落条目留痕→预警面板处置反馈承接；有意延后卷锚零消费→4.4 卷映射）。
- **4.4 预警雷达批立项**（bcb441c，方案件 `docs/work/4.4-预警雷达批-方案.md` 待作者审定）：目标=三张死表（expect/doNotReexplain/tripwires）接确定性消费→origin=radar 反向提案入统一收件箱+顶/列两档落地+重报锚卷映射消费+处置率仪表；边界=三态契约不动/severity 表达不改/预警零 LLM；三个方案内决策点待作者拍板（伏笔逾期判据数据源/置顶实现位/预警噪音阀门）。

## 本卷期间其他记档

- 测试基线 1117→1120（core 263 / domain 446 / shell 411 / cargo 19）；`npm run check` 0 错。
- GitNexus 索引修复与补核（7ac5e1a / fa0efba）：索引停 57c4e493（v0.2.12 时点）后 33 提交未重建+上会话误给 analyze 带 --repo（unknown option；「exit 0」为管道后 head 退出码假象）——仓根无参 analyze 44s 增量重建（4580 节点/14115 边），4.3 新符号 impact 实测可查；本机双仓注册表（AnySpark+final）查询命令 --repo . 必带；4.2.1/4.3 两批「人工调用链替代影响分析」的欠账用图级 retroactive 补齐（detect-changes compare=56 文件/317 符号/126 流 critical 即 v0.2.13+ 全部改动面；承重符号 impact 十项图面与人工结论全部吻合零漏面；check --cycles 唯一环 character-norm↔ledger 定性 import type 刻意反转非缺陷）。AGENTS.md/CLAUDE.md 生成块数字随 analyze 机械刷新。
- 工程环境实录：子代理通道恢复（三路并行评审可行，上会话不可用已解除）；e2e-workflow/promptfoo 需真实 LLM 因通道余额未跑（v0.2.13 同口径披露）；LLM 通道可用=OpenRouter minimax/minimax-m3:free。
- 启动交接件第二棒（交接-下会话启动-2026-09-05-2.md）消费完毕随本版封存归档，续点由现状.md 定序+4.4 方案件接棒。
