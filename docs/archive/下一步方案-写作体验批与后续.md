# 下一步方案:写作体验批 + 新空间 + 后续(0.1.6 亲测反馈)

> 性质:作者 0.1.6 真实写作反馈 8 项 + 此前挂起提议 4 项的合并方案。
> 日期:2026-08-15。

## 一、反馈根因与修法(写作体验批,优先)

| # | 反馈 | 根因 | 修法 |
|---|------|------|------|
| 1 | 对话没发送就关栏,草稿丢失 | composer 输入是 ChatColumn 局部状态,栏切换/收起即卸载 | 草稿移入 chat store,按会话/存区键控(`draftMap`),composer 改绑 store;卸载不丢 |
| 2 | 没有作者私房笔记空间 | 一切内容 AI 可经工具读到 | 新增「作者笔记」:存 `.novel/notes/`(作品一份 + 每章一份,随当前章切换);**读写只走 tauri fs 命令,不进 domain 工具集**——AI 物理不可达;UI 放结构树底部折叠区 |
| 3 | 修改弹窗让选择,想先进暂存区 | `inlineSplit`(小改就地分流)默认开,小改走 B1 浮层 | 默认改为暂存优先(inlineSplit 默认关);浮层保留为显式入口;设置项文案改为「小改就地浮层(默认进暂存区)」 |
| 4 | 审阅按钮不知道干嘛、有时点不开 | 扫描中按钮 disabled 无进度说明;错误只走红条;面板不写处理内容 | ① 面板顶部写清三段处理:便宜档 scan_quality(口水词/模板段)+ 账本确定性诊断 + 贵档冷读(按章);② 扫描中加进度文案与「可关闭」提示,失败内嵌面板不只看红条;③ 复现排查"点不开"(running 卡死/慢)并修 |
| 5 | 「打字机」按钮意义不明 | 命名不自解释 | 改名「光标锁」,title 写明:打字机滚动——光标行锁定在屏幕 42% 处,长文输入不追底 |
| 6 | AI 写完要按 F5 才看得到 | `reloadCurrent()` 只在手动放行时调;auto/yolo 直写后无任何刷新(chat.svelte.ts:256-263) | chat 轮次结束(onDone)时:本轮含结构变更工具(write/delete/create/rename/move 章/卷)即 `work.loadStructure()` 刷树;若 write_chapter 命中当前打开章,`work.reloadCurrent()` 以磁盘为准(沿用放行路径语义,脏编辑器处理与现有一致) |
| 7 | 快照不知道内容/数量,只能盲覆盖 | 顶栏快照只问"还原最新" | 快照浏览器(模态):`list_snapshots` 列出时间/章/大小,点击 `read_snapshot` 预览前 N 行,确认才还原;两工具 domain 已有,纯壳活 |

## 二、新空间(批一次之)

- **B2 上下文面板**(v3 材料唯一挂起项,账本已落地):右侧 AI 面板加「上下文」栏,展示四维账本(伏笔/道具/时钟/知情地图),数据走 `/v1/tools/ledger_read`(壳只读零耦合)。
- **与 #2 作者笔记的边界**:笔记=作者私房话,AI 物理不可见;上下文=AI 维护的设定账本,作者可审。两者同屏互补,设计时一起定位置,不做成一个东西。

## 三、后续(按需插队)

- **工具结果回放**:跨轮上下文从"工具摘要"升级"含结果"(25k 字符预算需重校;写作中若 AI 频繁"忘事"再提前)。
- **覆盖层宽度口径统一**(B1 浮层 580/审批卡 480/速查卡 340):纯打磨。
- **prod 包 core 自报 commit:"unknown"**:build-sidecar.mjs 注入 commit hash,一句话。

## 四、批次与验收

- **批一(#1-#7 写作体验)**:真实写作动线验收——AI 写完编辑器与树立即刷新;关栏草稿还在;快照可选可看;审阅按钮每点必应且说明清楚。
- **批二(#2+B2)**:笔记 AI 不可达验证(对话里让 AI 找笔记内容,应找不到);上下文栏显示账本四区。
- **批三/四**:写作验证提出需求时启动。
