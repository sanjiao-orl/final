# 现状·批史归档（至 v0.2.11）

> 2026-08-24 随 v0.2.11 发版按现状.md 随版归档纪律移入，冻结不改写。收录：块 2 声口主题批、修复批 R4（两批均已随 v0.2.11 发布）。核销明细见 `bugs-核销史-至v0.2.11.md`；过程留 git。

**块 2 · 声口主题批（已收口，2026-08-23，已随 v0.2.11 发布；明细见归档件核销史）**：前置断言先行（promptfoo 声口建档用例）+ 四件全落——①「声口建档」skill 转正（预置 3→4；判定改以 read_style 为事实源，取样可参照 voice_fingerprint 量化底数）。②style.md 全文可读回（domain `read_style` 第 37 工具，缺失返回 exists:false；摘要仍 1500 投影不变）。③声口指纹确定性检查（domain `voice_fingerprint` 第 38 工具：句长分布/对白占比/段长/高频二字组，纯计算；偏离阈值写死、样本 <100 CJK 不出提示）。④续写/改写口吻偏离检测（core continue/rewrite 的 done 附 voice，基线=正文尾巴/选区原文；壳候选详情卡「声口偏离」提示行——仪表非门禁，会话态快照不持久化）。分层纪律两条落地：事实源与投影分离（chat.md 指引钉死「声口摘要段只是投影，细看调 read_style」）；生成与检查分离（指纹归 domain 纯计算，建档 skill 只蒸馏）。校验出口=续写与改写采纳率（真实使用积累，挂北极星「成稿更快」，铺平 ghost-text 触发条件）。

**修复批 R4（已收口，2026-08-23，已随 v0.2.11 发布；明细见归档件核销史）**：弱点优先级清单定档后作者圈定推荐批，六件全结——D6 stage 目标章存在性校验（read_chapter 三态校验，无效路径引导不落库；chat.md 纪律行+shipped-hashes）、D7 跨行 quote 三处紧凑兜底（对账器/质检/pm-search，精确首过不变）、D4 账本切片注入 3000 预算闸、D10 固定端口 47832 收死 CSP（决策 0016；fatal 事件透传端口占用原因；防漂移 Rust 测试）、D11 裸联调 DEV 门禁、D9 字数双口径参考件。验证基线 993→1011、cargo 17→19、promptfoo 真跑（免费档覆盖）。教训入档：行为回归前先看 LLM 余额。
