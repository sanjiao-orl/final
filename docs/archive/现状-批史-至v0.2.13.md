# 现状件批史归档 · 至 v0.2.13（2026-09-05）

> 2026-09-05 随 v0.2.13 发版按「随版归档」纪律从 `docs/current/现状.md` 移入，冻结不改写。本卷记录 v0.2.12→v0.2.13 期间批次：块 4.0 定稿批、4.0+ 对照实验批、4.1 地基批、4.2 薄切片裁决回路批（0018 召回补测与终裁含）。

## 批史详录（2026-09-05 一日内连续收口）

- **4.0 定稿批**（✅ 0905，纯文档）：0017/0018 作者审定（0905 主代理多角度审查 P1×1/P2×6 落实 + 总设计三调整并入后照准），按文档流水线改写为 `docs/reference/05-账本与长篇连续性.md`、`06-完本书分层导入与事件骨架.md`（现状文体，含超域处置/坐标权威两规约）；草案原件、试车台批记录两件、块 4 前置调研三件归档。定序推进：带数字审 ✅ → 4.0+ 对照实验。
- **4.0+ 对照实验批**（✅ 0905，作者裁决「有条件通过」+ 召回补测兑现 + 0018 终裁通过；批记录 `docs/work/块4对照实验-4.0加-2026-09-05.md` 随本卷同期归档）：分层管线 vs 均匀深读（净本 v1.2 前 300 章同台，总成本 ≈383 calls 免费档单日）。读数：深调用 −65%（130 vs 375）、全本外推总量 −50%；quoteMissing 8.6% vs 17.4%；守门人真矛盾被同章确定性检测器零成本捕获（净本 LLM 重跑曾漏报）；未选区零命中验证台账诚实性；重大附带发现=免费档同章两次抽取重合率仅 8-30%（→4.1 提案机器的实证理由）。召回补测（`scripts/bench/recall-recheck.mjs`，零 LLM，锚级对账）：分层选区内 27.9% vs 基线 32.8%（−4.9pp 过 −10pp 门限）=选区内召回相当；区间投影使未精读区命中 16.6%（裸子串口径曾 0）——0017 区间语义覆盖价值首证；绝对水位 32.8% 仍受抽取稳定性限制（→4.3 词典制承接）。**0018 主体实施终裁通过**（主代理按建议裁决，作者授权）：实施=与 4.3 并行的预处理独立线（scripts 侧纯导生），消费端挂 4.5 后视基准；被否选项⑤（维持均匀深读）不复活。通道排障记档：opencode zen 余额不足、nemotron 超时，改走 OpenRouter 免费档（试车台同款）；`lib.mjs` 加 `repairInnerQuotes`（免费档长 JSON 值内未转义引号状态机修复）。
- **4.1 地基批**（✅ 0905，行为零变+契约加法，三刀）：①envelope.ts——FactEnvelope {type,payload,区间,锚,面}、区间代数四函数（开放端=+∞，过期≠矛盾）、ledgerToFacts/factsToLedger 双读归一化（真账本实测：基线臂 2429 条目 66ms、分层臂 1006 条目 27ms 双读零 diff）；②ledger-index.ts——最小索引层：buildLedgerIndex（名字/伏笔 id/角色倒排）+ indexedSliceFromIndex（区间生效过滤+类型配额预算裁剪 promise 40%/prop 25%/knowledge 25%/clock 10%，防单一类型独占——真账本第 1137 章 promise 1739 条曾挤光其他维度）+ queryByName；实测：基线臂第 1137 章切片 44-49 万字符→29,999 压进 3 万预算 12ms、四维齐注入（伏笔195/道具89/知情132/时钟1），分层臂 29,978 字符 2ms；③proposal.ts——提案机器核心：操作序列（ADD/UPDATE/DELETE/NOOP+证据锚+摘由，映射既有 LedgerOp 走 applyOps 管线）、validateProposal 权限面（protect 写闸=拦、摘由/证据闸）、applyProposal（幂等=upsert 键语义）、统一裁决状态机（pending→adopted/discarded，dismiss 理由枚举+有意延后卷锚重锚）、md 序列化 round-trip；④ledger_slice 加可选 budget 闸（传入走索引层裁剪并返回注入构成，缺省原全量渲染零变；38 工具契约加法）。提交：0eb19bb / 8cf4786 / b100415。
- **4.2 薄切片裁决回路批**（✅ 0905 代码全环，承诺·伏笔窄域；提交 b13593a / 01adfad / 3a71bad）：①domain 机器态——promise-prefilter.ts（确定性嫌疑句式预筛×账本对照，未登记候选显式计数=超域规约落地）+ inbox.ts（统一裁决收件箱 md 存储 `.novel/inbox.md`：append 幂等（pending 同 action:targetKey 去重）、adopt=权限面复核→upsertLedger 落账→回读验证（ADD/UPDATE 验在位、DELETE 验消失，修复过闸铁律 4）+验证结论持久化、discard=理由枚举必带+有意延后卷锚重报依据持久化）+ MCP 工具 promise_prefilter/inbox_append/inbox_list/inbox_decide；②core——/v1/scan/promise 扫描管线（预筛先行→逐嫌疑章便宜档 LLM 判定 new/retire→makeScanOps 确定性映射（id=P-S-<章键>-<序>，LLM 只给判定与摘句）→提案草稿入收件箱；零嫌疑不取模型；maxChapters 预算闸；非写作热路径）+ 03 协议契约登记；③壳——InboxStore 状态层 + InboxList 面板（左栏第三 tab 目录/暂存/收件箱 + Toolbar 徽章：批量裁决、驳回理由下拉必带、有意延后卷锚输入、回读验证与驳回理由持久化展示、扫描按钮+覆盖读数）+ CoreClient.scanPromise。剩余出口=真实写作裁决负担数据（挂 4.3 真人小段，负担实证不成立→4.3/4.4 降规格或停——0017 §9 重开权条款）。

## 本卷期间其他记档

- 测试基线 1011→1060（core 254 / domain 403 / shell 403 / cargo 19）；`npm run check` 0 错。
- 工程环境实录（0905）：子代理通道不可用（`builtin:zai-start-plan` 未配置，三种类型同报）——审查类工作主代理亲读替代；e2e-workflow 因 opencode zen 余额不足未跑（发版披露）。
- GitNexus 索引接入（作者裁决保留；check-docs 的 AGENTS.md 50 行检查豁免 gitnexus 生成块）。
