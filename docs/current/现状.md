# 现状

> 2026-08-22 重写，取代 08-16 版（已归档 `docs/archive/现状-2026-08-16版.md`；批一各件与护栏批的过程细节、审核实录、亲测清单全在归档件与 git，不再搬运）。本件只记当前事实、规范与规划。维护纪律：每批收口后更新本件，写完即提交。活跃缺陷归 `docs/current/bugs.md` 台账（入帐→核销→git 留过程）。

## 产品

网文连载单作者本地 AI 写作工作台（小说领域的 Cursor）：Windows 桌面、纯本地、无账号、BYOK。需求基线 `docs/reference/01-产品定义.md`（原则：人的方向 AI 的笔 / 结构即智能 / 可配置默认否）。

验收口径：以作者真实写作为唯一验收。止损线：长期无动力用它写完一章，判定假设不成立，体面终止（可剥离资产：账本/core sidecar/领域工具）。

## 架构

`core/`（Node sidecar：AI SDK v7 + MCP client + node:sqlite 会话 + 127.0.0.1 HTTP/SSE）+ `domain/`（MCP stdio 领域服务，38 工具）+ `shell/`（Tauri 2 + Svelte 5 + TipTap，v5 五栏 + 作者笔记）。提示词与 skill 统一 md 文件机制（解析序 NOVEL_PROMPT_DIR > 随包释放目录，dev 直读 `core/prompts/`；mtime 热重载；决策 0008）。决策记录 `docs/decisions/`（速查见文末）。

## 验证基线（最新已发版 **v0.2.11**，2026-08-24：块 2 声口批 + 修复批 R4，NSIS/sig/latest.json 三资产核验过；更早版本史见 git tag 与归档件。未发版已收口：无——下一批从干净基线起步）

`npm run check` 0 错（含 check-versions 六处一致）、`npm test` 全绿 1011（core 249 / domain 364 / shell 398；修复批 R4 后，较 993 +18）、`cargo test` 19 绿、`node core/scripts/e2e-workflow.mjs` 12 步（块1 由 10 扩 12：+摘要生成回读 +质检；真实 LLM，key 在场才跑；2026-08-23 已复跑成功，块 1 以来端到端回归欠账清零）。行为回归新通道：`npm run promptfoo`（真实 LLM 四用例：起草路由/skill 铁律/非正文负向/声口建档全流程，措辞改动或发版前手动跑，不进 CI；2026-08-23 扩四用例并真跑 4/4，见 `scripts/promptfoo/README.md`；R4 批复跑撞 GPT_LUNA 余额不足，`LLM_ASSIGN_WRITING=OX_ALPHA_FREE` 覆盖跑通——余额先看再跑）。发布通道：bump→tag→gh 草稿→三资产（NSIS/sig/latest.json）→draft=false，幂等可续传；CI 门禁 `.github/workflows/ci.yml`（push main + PR + 手动，同 ref concurrency 取消旧构建，gate 30min 超时，windows-latest）。已知坑（已修）：runner autocrlf 检出 CRLF 曾把 prompt 升级通道 hash 比对打挂——contentHash 已行尾归一；重 CLI 进 devDependencies 会把 @types/* 提升进根目录打挂 workspace tsc（promptfoo 实证，改 npx 钉版本）。

## 规范（本轮起生效）

- 版本号：六处事实源（tauri.conf.json / Cargo.toml / core·domain·shell package.json / package-lock.json 的 workspaces 三段；Cargo.lock 的 app 段顺带同步），core/domain 代码读 package.json；`npm run check` 内含 `check-versions.mjs`，release.mjs 写完自检，漂移即失败。
- 提交：conventional commits 中文摘要；一切 git 写操作（commit/push/release）须作者明确指令。
- 文档：活跃件=本件 + `docs/current/bugs.md`；`docs/reference/01-产品定义.md`、`docs/reference/02-字数与统计口径.md`、`docs/decisions/`；历史入 `docs/archive/` 且不改写；新决策按序号入 `docs/decisions/`；文档改动后跑 `npm run check:docs`；作者口头强调的需求与审计发现的弱点当场登记进「弱点与需求登记」节，具体可定位缺陷入 bugs.md（0014/2026-08-22 分工）。**本件随版归档纪律（2026-08-24 立，同 bugs.md 口径）**：发版时把已发版批次的「当前规划」收口段、已全结的登记条、验证基线的旧版本枚举移入 `docs/archive/现状-批史-至<v 版>.md`，本件只回答「现在是什么、现在做什么」——防膨胀的机制是归档，不是加新容器；「弱点与需求登记」「缓做」必须留在本件（日常加载件）内防基线重置。
- prompt/skill：文件机制见 0008；预置 skill 无数量硬顶，书级试跑 + 真实写作验证留住才转正（0009），候选试跑架 `core/prompts/candidates/`（flat 扫描不触及）；结构级检查（伏笔/时间线/口吻/知情权）永远是 domain 确定性工具的职责，不写成 skill。
- 安全口径：账本/问题日志路径白名单（`.novel/` 根下或 `editorial_notes/` 下 .md）；apiKey 打码回传 + 占位即保留；连接 token 不进日志；webview CSP 兜底 + LLM/markdown 输出渲染前过 DOMPurify 白名单净化（`shell/src/lib/sanitize.ts` 统一入口，2026-08-23 评审提前项）；workDir 清洗后才许拼系统提示。
- 验证：改完必须 check/test/实跑，无法验证就明说；主 agent 审核不盲信回馈结论；发版前过亲测清单。
- 新阶段结构纪律（0011）：人审内容留 md、机械态/导生态可入 sqlite 或缓存（按「谁会编辑它」分流）；导生物可焚可重建；预处理走「脚本 → 导生物 → 主程序只读」，不拆仓；36 个 MCP 工具契约不动只做加法；新增理解层维度必须带证据锚 + 至少一条确定性诊断 + 注入预算上限。

## 弱点与需求登记（决策 0014 立；作者口头强调的需求当场登记，防会话间被基线重置；AI 执行中发现决策可疑可登记异议入本节）

分工：本节管系统性弱点与方向性需求；具体可定位缺陷（带坐标、可核销）归 `docs/current/bugs.md`（2026-08-22 立，首批=四路全链路排查 30 条：工具调用链超时取消族 P0 ×10 / domain 数据正确性 / skill·prompt 口径，修复由块 0 承接）。

活跃条目（已完成核销的见归档件——v0.2.10 前的 R2 能力批 T9-T14、流程安排三条已移 `docs/archive/现状-批史-至v0.2.10.md`）:
- 多模型矩阵（作者多次口头强调）→ 块 3（方案内模型绑定，待决策 0015）。
- R2 档位与路由不足（composer 只有写作/背景两档，要多档位切换）→ 块 3 一并承接。
- 运行时自诊断薄弱：空 catch 余 18 处（评审时约 60，块 0 随超时族已清沿途；2026-08-23 实测复核）、日志裸 console.* 无级别无关联 ID——块 0 已带级别日志与全链取消入口（审阅卡死零信号的根因已除），根治缓做。
- ledger.ts 约 1950 行单文件（批三启动前切一刀）；无 lint/format、测试缺故障注入、工具注册契约枚举测试 → 缓做随触及随拆。（build-sidecar 先删后拷已定位坐标，转 bugs.md [评审T7]）
- [评审R2 能力批]：T9-T14 已全数核销（2026-08-23，明细见归档件与 bugs.md）。
- [评审-前端系统性弱点]（前端审查报告主诊断）：「发现→到达」链路已由 T9 收口（壳层跳转公约 jump-target + 四类面板定位钮）；五栏互斥与「写→问→核对→改」工作流错配（v6 候选：双栏并置或窄条徽标+回跳，留真实写作数据裁决）；AI 状态/注入可见性碎片化（窄条无流式/审批/账本异常徽标；chat 无「本次注入了什么」清单——「笔记 AI 不可见」强承诺的信任对称性缺半）；大组件六件随触及随拆（TreeView 977/Editor 998/ReviewPanel 776/Toolbar 740/ChatColumn 627/SettingsColumn 564，拆分点已在报告标注，2026-08-23 复测）；零组件/DOM 测试（21 个测试文件全在 lib 层）。
- [评审R3 缓做]（触发条件见任务单，此处逐条登记防散失；D4/D6/D7/D9/D10/D11 已随修复批 R4 核销，见 bugs.md「R4 修复批核销」）：D1 SSE 无心跳帧；D2 SSE 断连无重连续传（retry/Last-Event-ID）；D3 非章挂载会话零数据层注入；~~D4 账本切片注入无预算上限~~（R4：3000 预算闸+指路 ledger_chapter_slice 自取）；D5 chat.ts 899 行拆分（批三前，同 ledger.ts 口径）；~~D6 append 编号全文匹配跳号~~（R4：stage 目标章存在性校验，无效路径引导不落库）；~~D7 跨行 quote 定位必失配~~（R4：domain/core/shell 三处匹配统一紧凑兜底，精确首过不变）；D8 rename/move 改 fm 无历史快照；~~D9 字数双口径（countWords vs countCjk）文档说清~~（R4：docs/reference/02-字数与统计口径.md）；~~D10 CSP connect-src 通配端口→动态 CSP~~（R4/决策 0016：固定端口 47832 收死，Tauri 2 无运行时 CSP 属实证约束）；~~D11 裸联调 token query/localStorage 加 DEV 门禁~~（R4：import.meta.env.DEV 门禁，生产不读不写）；D12 ~~marked 输出显式净化~~（已提前单独小批，见「评审-流程安排」②）；D13 e2e 与壳内采纳路径同源化；D14 日更目标/断更警示/存稿箱/发布队列；D15 平台格式复制多平台差异；D16 续写 append 候选无内联对照；D17 声口/摘要 1500 硬顶可视化（并已有「全书状态面板」缓做项）；D18 search_content 加正则；D19 冷读提示词双契约拆分；D20 批量采纳跨章强切编辑现场。
- [四维账本长篇承载力]（作者挂记，2026-08-24 评估取证）：记账-对账-诊断骨架可担纲（四维 schema+证据锚+10 条确定性诊断+切片防泄露过滤+写入 CAS/快照，均实证）；四个缺口按咬人序——①**漏登记不可自愈**：对账器只验已记条目的锚，不验「该记未记」，完备性全靠 LLM 自觉+作者手动；章摘要与账本零关联字段，回溯补账通道缺。②**人物维度整体缺席**：character/holder/knownBy 全自由字符串无归一化（人名写错=新人物），性格/目标/关系无承载；「角色卡在批三」的批三概念已亡，角色卡成规划孤儿，需先给归属。③**预警有雏形不常规化**：dangling/overdue 诊断存在但 overdue 需手填 due、〔预计回收卷已过〕只是渲染标记非 finding；doNotReexplain/protect/tripwires 三张表零代码消费（只随切片透传给冷读）。④**规模未测**：账本整文件 YAML round-trip、对账器逐锚读章且同章双读无缓存、冷读 ledger_slice 全文无预算（D4 只闸了 chat 章切片）、perf-benchmark 不生成账本场景。缓补方向（不越 0011「不做知识图谱/状态机」红线，全走确定性诊断+证据锚+LLM 提案作者裁决）：对账同章缓存+冷读预算闸（快赢）→ 补账扫描借摘要+冷读出提案清单（中件）→ 角色卡立决策件定归属（大件）→ perf-benchmark 补账本基准。→ **块 4 立项承接（2026-08-24 作者定，结构见「当前规划」）**。升级光谱定调（对表 0004「索引库接口预留不实现」/0011 事实源分流）：职责面已超载该升（角色卡复活 0011 批三第8条完整方案+补账扫描+预警常规化），性能面小步升（同章缓存+冷读预算闸+基准），存储面等数字（导生索引层挂基准触发；账本迁 sqlite 不做——人审内容留 md；embedding 不做——触发零记录）。

文档自述偏差（审计实证，对外表述勿夸大）:
- 碰撞模式=容错着色的格式协议，四节靠模型自觉，非强校验工作流（失败率超阈值升 Output.array，0013 已留后路）。
- personas 是 AI 工作姿态，不是小说角色卡（角色卡在批三）。
- 采纳留痕=候选信息展示，非完整审计链；裁决留痕=md 追加行，非可检索决策系统。

## 当前规划（2026-08-22 定稿，取代原「新阶段双轨三批」待办；依据：本轮整体复盘 + 作家助手写作层调研，见 `docs/archive/作家助手写作层调研-2026-08-22.md`）

**节奏**：快速成块 → 实操撞反馈 → 检修 → 下一块。不停车等验收；方向类决策已钉死，深度类决策留实操数据（分支条件预建，数据落位即触发）。每块出口挂北极星双指标（成稿更快了吗 / 离谱更少了吗）作仪表，不作门禁。

**已发版批次史已随版归档：v0.2.10 前的七批见 `docs/archive/现状-批史-至v0.2.10.md`；块 2 声口批与修复批 R4（v0.2.11）见 `docs/archive/现状-批史-至v0.2.11.md`。本节只留在排规划。**

**块 3 · 模型方案绑定（0015）+ 碰撞深化**：批二后、真三方会审前（0014 定序）。细则已起草待作者审定：`docs/decisions/0015-模型方案内绑定.md`（稿——按用途槽绑定/壳解析 core 收字段/兜底显式化/档位两轴论/批 3a 数据前置 mode·tier 落账/碰撞深化等数据另立项，含对 0010「单通道不做多人格」的措辞修订与前端触点清单）。碰撞深化深度由实操数据定（开舱率低则从简）——3a 数据前置即其依据采集。

**块 4 · 长篇连续性阶段（2026-08-24 立项，作者定；四维账本承载力登记条的承接；结构=外整内快——阶段统一设计、批内小步垂直可发版）**：4.0 定稿批（决策 0017：角色卡 schema（0011 批三第 8 条方案复活+修订）/补账提案协议/预警分级/摘要-账本关联字段/索引层触发阈值 + 前端触点与 UI 形态统筹 + 断言先行，0011 三件套纪律逐件过）；4.1 地基批（ledger.ts 2030 行切刀、chat.ts 拆分（D5 核销）、对账器同章缓存、冷读 ledger_slice 预算闸、perf-benchmark 补 300 章合成账本基准——行为零变独立发版，登记的「批三启动前切一刀」时限由此兑现）；4.2 角色卡批（domain schema+工具加法+人名归一化诊断 → core 注入预算 → 壳 ContextColumn 角色面板）；4.3 补账批（摘要加关联字段 → 冷读双契约拆分（D19 核销）→ 扫描「正文有而账本未记」提案清单 → 壳确认流，作者裁决才写账本）；4.4 预警雷达批（overdue 的 due 字段引导、doNotReexplain/protect/tripwires 三张死表接确定性消费 → 审阅面板常规输出+可见面）；4.5 收口（基准复测、北极星报数——anchors 计数与补账漏登记数即新仪表；导生索引层视基准数字进 4.6 或挂触发）。阶段纪律：每批独立可发版测试全绿、4.2-4.4 按真实写作先撞到的重排、38 工具契约只加法、事实源留 md 两条红线全程不碰。与块 3 先后待作者定。

**机制层（随手做，不占块）**：新决策落档带置信度+重审触发条件+被否选项（旧决策不重写）；AI 异议登记通道=弱点与需求登记节；bugs.md 生命周期三件套。

**亲测留意**（日常使用顺带，不再列清单门禁）：碰撞四节遵循率与裁决登记自觉性、续写质量与留住率、暂存提案路由（正文进暂存区/设定走 ledger）、方案 pill 切换后三通道姿态变化、日历口径与平台粘贴、候选详情卡「声口偏离」提示的准头与噪音（阈值写死在 domain/src/voice.ts，吵了/漏了就登记 bugs.md）、固定端口 47832 被占时的报错体验与同机双开行为（决策 0016 代价面）。异常随时登记。

## 缓做（触发条件驱动）

skill 管理 UI（skill 多到管不过来）、~~promptfoo 回归~~（已建成转正，见「块 2 前置 · 两小批」②）、角色/方案壳内编辑 UI（改文件改烦）、确定性治理可调化（真实误报/漏报；方向：书级抑制阀→词表数据化→规则注册表）、语义索引/embedding（0004/0006/0009 后置；触发：真实查询失败被记录——作家助手「问全书」是 RAG 路线，不构成触发）、会话压缩摘要层（超预算会话占比实测超标）、ghost-text 行内续写（触发式续写留住率达标——经块 2 声口偏离检测）、全书状态面板（状态数据源 ≥3 且散在多处）、workDir 根白名单（导入外部文本或多作品并发）、随机取名器（进 candidates 试跑架）、锁屏码字（明确不做，与 F8 专注重叠）、导入既有作品（首次需导入老书）、作品备份迁移（单书过 10 万字或换机——sqlite 冷拷贝由块 0 提前兜底）、编辑器崩溃恢复（真实丢稿事件）、lint/format 与故障注入与契约枚举与结构化日志（CI 落地后统一评估）。

## 明确不做

图形化世界观、多人协作、模型市场、EPUB 排版、AI 绘图、治理体系、跨卷移动、壳内指令模板搬家（0009）、预处理操作台（工程概念不泄漏给作者，0011）、全书自动建知识图谱（0011）、游戏式单一权威实时状态机（0011）。

## 决策速查（`docs/decisions/`）

做判断前先查这里，每条一句：
- 0001 harness 选型：Vercel AI SDK v7，不手写 agent 内核
- 0002 接线：Node sidecar + HTTP/SSE，不过 Tauri IPC
- 0003 壳基线：Tauri 2 + Svelte 5 + TipTap（布局快照已演进为 v5 五栏）
- 0004 数据模型与领域服务：domain MCP（结构即智能的载体）
- 0005 暂存区与讨论存区：归属 core sqlite
- 0006 修复轮：周 2-3 审核后质量修复（「updater 占位」已被真实通道取代）
- 0007 协议契约 v1：/v1/ 版本化 + 进程握手 + event_pump
- 0008 提示词与 skill 统一文件机制：md 文件单一事实源，改文件即生效（热重载已落地；domain 侧 cold-read 永久缓存口径分裂在修，bugs.md）
- 0009 理解层闭环与内在深化路线：批三三批已完成；skill 预置无硬顶、书级试跑转正
- 0010 角色与方案两层姿态注入：方案=角色×通道分配表（书级多套+指针随书）；注入面分契约/姿态/数据三层；确定性检查不被方案配置（批一①已落地）
- 0011 新阶段：能力对齐与打磨——北极星双指标校验门（成稿速度/离谱率）；挂起清单触发驱动
- 0012 铁律回归：chat 正文经本地工具 stage_chapter_proposal 进暂存区（kind=replace/append/replace_all，采纳才落盘）；write_chapter 仅作者明确直写时用（审批门保留）；设定→ledger_upsert、书级元数据→write_meta，严禁写 manuscript
- 0013 吸收人机碰撞方法论：外部路线 B 冻结为方法论库；裁决留痕 `editorial_notes/decisions.md`；章 frontmatter 加 `blueprint` 状态位；证据锚统一 `evidence:{chapter,line,quote}`；摘要机检字段冻结；方案只管通道×角色（已被 0014 修订为方案可绑模型）；壳白名单 4 处；回放预算提级至碰撞之前
- 0014 外部审计与规划重排：模型进方案矩阵（修 0010/0013 方案边界）；校验门仪表化；弱点与需求登记制
- 0015（待决）：模型方案内绑定细则 + R2 多档位诉求 → 块 3 定稿
- 0016 固定端口与 CSP 收口：core 固定 127.0.0.1:47832（修 0002 随机端口）、CSP 收死通配、裸联调 DEV 门禁；重审触发=多作品并发落地时端口仲裁

## 文档地图

- 日常唯一加载：`docs/current/`（本件 + bugs.md 台账）。
- 按需加载：`docs/reference/`（需求基线等稳定件，带 YAML front matter）、`docs/decisions/`（按序号单条读）。
- 历史：`docs/archive/`；逐件一句索引见 `docs/archive/README.md`，一律不改写。
